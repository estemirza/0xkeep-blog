---
import Layout from '../layouts/Layout.astro';

// 1. Grab all Markdown files
const allPosts = await Astro.glob('./posts/*.md');

// 2. Sort them by date (Newest first)
const sortedPosts = allPosts.sort((a, b) => {
    return new Date(b.frontmatter.date).valueOf() - new Date(a.frontmatter.date).valueOf();
});
---

<Layout title="All Articles | 0xKeep Blog">
    
    <div class="text-center mb-16 fade-in-up">
        <h1 class="text-4xl md:text-6xl font-light tracking-tight mb-4 text-white">
            All Transmissions
        </h1>
        <p class="text-base md:text-lg text-zinc-400 font-light max-w-2xl mx-auto leading-relaxed">
            The complete archive of 0xKeep research, updates, and news.
        </p>
    </div>

    {sortedPosts.length === 0 ? (
        <div class="glass-card p-10 text-center text-zinc-500 italic">No articles found.</div>
    ) : (
        <!-- Grid of all articles -->
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 fade-in-up delay-100">
            {sortedPosts.map((post) => (
                <a href={post.url} class="glass-card group flex flex-col justify-end h-full hover:border-purple-500/50 transition-all duration-300 min-h-[300px] relative overflow-hidden">
                    
                    <!-- Background Image & Overlay -->
                    {post.frontmatter.image && (
                        <img src={post.frontmatter.image} alt={post.frontmatter.title} class="absolute inset-0 w-full h-full object-cover z-0 opacity-40 group-hover:opacity-60 group-hover:scale-105 transition-all duration-700" />
                    )}
                    <div class="absolute inset-0 bg-gradient-to-t from-[#030305] via-[#030305]/90 to-[#030305]/20 z-10 pointer-events-none"></div>

                    <!-- Content -->
                    <div class="relative z-20 p-8 flex flex-col h-full">
                        <div class="flex flex-wrap gap-2 mb-4">
                            {post.frontmatter.tags && post.frontmatter.tags.map((tag: string) => (
                                <span class="font-mono text-[10px] text-blue-400 uppercase tracking-widest border border-blue-500/20 rounded px-2 py-1 bg-blue-500/10">
                                    {tag}
                                </span>
                            ))}
                        </div>
                        
                        <h3 class="text-2xl font-medium mb-3 text-white group-hover:text-blue-300 transition-all duration-300">
                            {post.frontmatter.title}
                        </h3>
                        
                        <p class="text-zinc-400 text-sm leading-relaxed mb-6 line-clamp-3">
                            {post.frontmatter.description}
                        </p>
                        
                        <div class="flex justify-between items-center pt-6 border-t border-white/10 mt-auto">
                            <div class="font-mono text-xs text-zinc-500">
                                {new Date(post.frontmatter.date).toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric' })}
                            </div>
                            <div class="text-purple-400 group-hover:translate-x-2 transition-transform duration-300">
                                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14 5l7 7m0 0l-7 7m7-7H3"></path></svg>
                            </div>
                        </div>
                    </div>
                </a>
            ))}
        </div>
    )}

</Layout>